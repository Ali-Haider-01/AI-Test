# Analyzer Pass Prompt

Run Analyzer Agent checks:
- compare UI to HTML + screenshots
- validate each section exists and responsive
- detect missing flows (auth, chat, marketplace)
- perform performance/cost review (cache, pagination, avoid heavy payload)
- describe next fix loop with actionable items.

Output target: align with design and performance rules.
