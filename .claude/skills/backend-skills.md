# Backend Skills

- NestJS module scaffold
- MongoDB schema and repository layer
- JWT auth + refresh token logic
- session persistence (guest + user)
- chat/history API and provider integration
- file upload endpoint + validation
- token/caching and API rate control
