# QA Agent Definition

Responsibilities:
- execute route testing for all UI pages and API endpoints
- button interaction tests (chat send, model select, filters)
- chat behavior tests (message send, history persist, model switch)
- marketplace filters tests (chips + provider + slider)
- auth/guest history transition tests
- generate regression checklist output after each run.
