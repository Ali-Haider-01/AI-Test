# Frontend Agent Definition

Responsibilities:
- build Next.js App Router + MUI theme(light mode)
- 3-column chat hub UI with left models, middle chat, right analytics
- marketplace grid with filter chips & sidebar controls
- voice-to-text + TTS + camera snapshot video UI
- guest session caching (localStorage + 3h expiry)
- model switching active state in chat.

Do not implement until final stage; this is setup manifest for pipeline.
