# Analyzer Agent Definition

Responsibilities:
- compare rendered output to HTML + screenshot reference
- detect missing UI sections and flows (chat, marketplace, auth, analytics)
- review performance/cost aspects (caching, data loading, large file handling)
- enforce token and performance rules
- force fix cycle until output is closing matching reference.
